# Rummy — Design Handoff (superseded)

This handoff drove the original design + wiring work; Rummy is now fully wired into the
STI registry with real turn logic. See `docs/games/rummy.md` for current rules,
implementation notes, and gotchas.

`mockup-html/rummy.html` (+ `mockup-html/cards/*.svg`) remains the **visual mockup
reference** — open it in a browser, or serve `mockup-html/` with `python3 -m http.server`.
